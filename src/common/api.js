import { domain } from '@/config'
const api = {
  getBarList: `${domain}/sunbar-bar-service/bar/list`,
  BarDetail: `${domain}/sunbar-bar-service/bar/`,
  barSearch: `${domain}/sunbar-bar-service/bar/list`,
}

export default api